diff --git a/luci-app-ssr-plus/Makefile b/luci-app-ssr-plus/Makefile
index 8be2a1e..e9482d0 100644
--- a/luci-app-ssr-plus/Makefile
+++ b/luci-app-ssr-plus/Makefile
@@ -48,6 +48,7 @@ LUCI_DEPENDS:= \
 
 
 define Package/$(PKG_NAME)/config
+if PACKAGE_$(PKG_NAME)
 
 choice
 	prompt "Transparent Proxy Backend"
@@ -170,6 +171,7 @@ config PACKAGE_$(PKG_NAME)_INCLUDE_ShadowsocksR_Libev_Server
 	bool "Include ShadowsocksR Libev Server"
 	default n
 
+endif
 endef
 
 
